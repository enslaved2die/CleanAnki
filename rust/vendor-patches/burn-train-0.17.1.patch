diff -ruN --ex a/burn-train/src/metric/processor/async_wrapper.rs b/burn-train/src/metric/processor/async_wrapper.rs
--- a/burn-train/src/metric/processor/async_wrapper.rs	2026-07-10 23:55:50
+++ b/burn-train/src/metric/processor/async_wrapper.rs	2026-07-10 23:55:50
@@ -1,54 +1,98 @@
-use super::{Event, EventProcessor};
-use async_channel::{Receiver, Sender};
+// WASM SPIKE (CleanAnki): the original AsyncProcessor spawns an OS thread and
+// uses async_channel's `send_blocking`/`recv_blocking`, which are cfg'd out on
+// wasm targets (blocking is disallowed). On wasm we process metric events
+// synchronously in-place instead — burn-train is only reachable via fsrs's
+// training path, which the wasm bridge never exercises, so this only needs to
+// compile + link. The native implementation is unchanged.
 
-pub struct AsyncProcessor<P: EventProcessor> {
-    sender: Sender<Message<P>>,
-}
+#[cfg(not(target_family = "wasm"))]
+pub use threaded::AsyncProcessor;
+#[cfg(target_family = "wasm")]
+pub use wasm_sync::AsyncProcessor;
 
-struct Worker<P: EventProcessor> {
-    processor: P,
-    rec: Receiver<Message<P>>,
-}
+#[cfg(not(target_family = "wasm"))]
+mod threaded {
+    use async_channel::{Receiver, Sender};
 
-impl<P: EventProcessor + 'static> Worker<P> {
-    pub fn start(processor: P, rec: Receiver<Message<P>>) {
-        let mut worker = Self { processor, rec };
+    use super::super::{Event, EventProcessor};
 
-        std::thread::spawn(move || {
-            while let Ok(msg) = worker.rec.recv_blocking() {
-                match msg {
-                    Message::Train(event) => worker.processor.process_train(event),
-                    Message::Valid(event) => worker.processor.process_valid(event),
+    pub struct AsyncProcessor<P: EventProcessor> {
+        sender: Sender<Message<P>>,
+    }
+
+    struct Worker<P: EventProcessor> {
+        processor: P,
+        rec: Receiver<Message<P>>,
+    }
+
+    impl<P: EventProcessor + 'static> Worker<P> {
+        pub fn start(processor: P, rec: Receiver<Message<P>>) {
+            let mut worker = Self { processor, rec };
+
+            std::thread::spawn(move || {
+                while let Ok(msg) = worker.rec.recv_blocking() {
+                    match msg {
+                        Message::Train(event) => worker.processor.process_train(event),
+                        Message::Valid(event) => worker.processor.process_valid(event),
+                    }
                 }
-            }
-        });
+            });
+        }
     }
-}
 
-impl<P: EventProcessor + 'static> AsyncProcessor<P> {
-    pub fn new(processor: P) -> Self {
-        let (sender, rec) = async_channel::bounded(1);
+    impl<P: EventProcessor + 'static> AsyncProcessor<P> {
+        pub fn new(processor: P) -> Self {
+            let (sender, rec) = async_channel::bounded(1);
 
-        Worker::start(processor, rec);
+            Worker::start(processor, rec);
 
-        Self { sender }
+            Self { sender }
+        }
     }
-}
 
-enum Message<P: EventProcessor> {
-    Train(Event<P::ItemTrain>),
-    Valid(Event<P::ItemValid>),
+    enum Message<P: EventProcessor> {
+        Train(Event<P::ItemTrain>),
+        Valid(Event<P::ItemValid>),
+    }
+
+    impl<P: EventProcessor> EventProcessor for AsyncProcessor<P> {
+        type ItemTrain = P::ItemTrain;
+        type ItemValid = P::ItemValid;
+
+        fn process_train(&mut self, event: Event<Self::ItemTrain>) {
+            self.sender.send_blocking(Message::Train(event)).unwrap();
+        }
+
+        fn process_valid(&mut self, event: Event<Self::ItemValid>) {
+            self.sender.send_blocking(Message::Valid(event)).unwrap();
+        }
+    }
 }
 
-impl<P: EventProcessor> EventProcessor for AsyncProcessor<P> {
-    type ItemTrain = P::ItemTrain;
-    type ItemValid = P::ItemValid;
+#[cfg(target_family = "wasm")]
+mod wasm_sync {
+    use super::super::{Event, EventProcessor};
 
-    fn process_train(&mut self, event: Event<Self::ItemTrain>) {
-        self.sender.send_blocking(Message::Train(event)).unwrap();
+    pub struct AsyncProcessor<P: EventProcessor> {
+        processor: P,
     }
 
-    fn process_valid(&mut self, event: Event<Self::ItemValid>) {
-        self.sender.send_blocking(Message::Valid(event)).unwrap();
+    impl<P: EventProcessor + 'static> AsyncProcessor<P> {
+        pub fn new(processor: P) -> Self {
+            Self { processor }
+        }
+    }
+
+    impl<P: EventProcessor> EventProcessor for AsyncProcessor<P> {
+        type ItemTrain = P::ItemTrain;
+        type ItemValid = P::ItemValid;
+
+        fn process_train(&mut self, event: Event<Self::ItemTrain>) {
+            self.processor.process_train(event);
+        }
+
+        fn process_valid(&mut self, event: Event<Self::ItemValid>) {
+            self.processor.process_valid(event);
+        }
     }
 }
