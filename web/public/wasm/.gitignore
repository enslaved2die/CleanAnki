# Generated by web/scripts/sync-wasm-artifacts.sh from rust/wasm-bridge's
# build output — reproducible, not source. The .wasm alone is ~11MB; keeping
# it out of git history is deliberate (see docs/ARCHITECTURE.md §8).
*
!.gitignore
